import { CreateFormValidator } from './create-form-validator';

describe('CreateFormValidator', () => {
  it('should create an instance', () => {
    expect(new CreateFormValidator()).toBeTruthy();
  });
});
